BEGIN;

CREATE TYPE posts_state AS ENUM ('DRAFT', 'PRIVATE', 'PUBLIC');

CREATE TABLE posts (
    id BIGINT NOT NULL GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    state posts_state NOT NULL DEFAULT 'DRAFT'
);

COMMIT;
