syntax = "proto3";

package irtest.pg_override_requires_text;

import "w17/db.proto";
import "w17/field.proto";
import "w17/pg/field.proto";
import "w17/pg/project.proto";

option (w17.pg.project) = {
  custom_types: [
    { alias: "jsonb_custom", sql_type: "JSONB" }
  ]
};

message X {
  option (w17.db.table) = { name: "xs" };

  int64  id    = 1 [(w17.field) = { type: ID, pk: true, default_auto: IDENTITY }];
  string title = 2 [
    (w17.field)    = { type: CHAR, max_len: 200 },
    (w17.pg.field) = { custom_type: "jsonb_custom" }
  ];
}
